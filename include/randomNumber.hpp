#ifndef RANDOM_NUMBER_HPP
#define RANDOM_NUMBER_HPP

#include <random>

namespace RandomNumber
{
	//generates a random number between x-y (both are inclusive)
	int generateRandomNumber(int x, int y);
};

#endif
