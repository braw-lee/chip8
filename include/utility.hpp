#ifndef UTILITY_HPP
#define UTILITY_HPP

#include <iostream>
#include <string>

namespace Utility
{
	//convert an int to a string in readable hex form
	std::string toHex(int num, int length);
};

#endif
